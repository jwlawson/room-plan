<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.co.jwlawson.plan"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" android:targetSdkVersion="15"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity android:name=".activities.RoomViewActivity" android:theme="@style/Theme.Sherlock">
            
        </activity>
        <activity android:name=".activities.OptionsActivity" android:theme="@style/Theme.Sherlock"></activity>
        <activity android:name=".activities.RoomList" android:theme="@style/Theme.Sherlock"></activity>
        <activity android:name=".activities.PegViewActivity" android:theme="@style/Theme.Sherlock">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:theme="@style/Theme.Sherlock" android:name=".activities.DimViewActivity"></activity>
    </application>

</manifest>